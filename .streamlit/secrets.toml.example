# Streamlit local secrets template.
# Copy this file to secrets.toml only on the machine or host that runs Streamlit.

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_ANON_KEY = "your-anon-key"
